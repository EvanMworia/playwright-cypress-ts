//correctly type variables for a scanId (number), targetUrl (string), and isCompleted (boolean).
let scanId: number;
let targetUrl: string;
let isCompleted: boolean;
// Assign appropriate values to each variable
scanId = 12345;
targetUrl = 'https://example.com/scan';
isCompleted = false;
console.log(`Scan ID: ${scanId}, Target URL: ${targetUrl}, Is Completed: ${isCompleted}`);
